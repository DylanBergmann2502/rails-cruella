#!/bin/bash
# deploy/production/compose/rails/sidekiq/worker/start

set -o errexit
set -o pipefail
set -o nounset

# Wait for postgres and redis to be ready
rails db:prepare

# Run Sidekiq worker
exec bundle exec sidekiq
